import React from "react";
import Layout from "../components/Layout";

const teams = () => {
	return (
		<Layout>
			<h1>Teams</h1>
			{/* Scrapping this page for now... we can move to a single page look later, but for now I am going to just split these into separate teams */}
		</Layout>
	);
};

export default teams;
